# __PROJECT_TITLE__

Minimal React 19 + TypeScript 6 + Vite SPA. Add router, API, shared UI, and feature layers only when needed.

Requires Node.js ^20.19.0 or >=22.12.0.

```bash
npm install
npm run dev
npm run typecheck
npm run lint
npm run build
```

The generator does not install dependencies, initialize Git, or include proprietary packages.
